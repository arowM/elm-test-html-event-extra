{
  "private": true,
  "scripts": {
    "format": "elm-format src",
    "start": "elm-doc-preview --port=8110",
    "review": "elm-review --fix",
    "test": "elm-review && elm-format --validate src && elm make --docs=/tmp/docs.json"
  },
  "dependencies": {
    "elm": "^0.19.1-5"
  },
  "devDependencies": {
    "@elm-tooling/elm-language-server": "^2.7.0",
    "elm-doc-preview": "^5.0.5",
    "elm-format": "^0.8.7",
    "elm-review": "^2.10.2"
  }
}
